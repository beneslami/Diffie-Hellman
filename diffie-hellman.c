#include <stdio.h>

// private key generator

// compute the generator G=(a,b)---> find the number of iteration
/*
slope = (3x^2 + a)/2y
x_i = slope^2 - 2x
y_i = slope(x - x_i)-y
*/

// iterate the above formulas until the it limits to the infinity. the number of iteration is what we want for the next step.

//scalar multiplication computer -> number

// send the number to each other -> number2

//each party should multiply the received number with its own private key
